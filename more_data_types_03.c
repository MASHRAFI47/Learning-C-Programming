#include <stdio.h>

int main () {
    // data types
    // 1. basic data types: int, float, double, char, void
    // 2. Derived Data Types: arrays, unions, enum, pointers, struct

    //array
    int numbers[20]; //this is an array of numbers which containes 20 numbers

    //structure 
    struct person {
        char name[50];
        int age;
    }

    return 0;
}